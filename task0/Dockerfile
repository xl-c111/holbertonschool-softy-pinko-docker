# Use latest Ubuntu as base image
FROM ubuntu:latest
# Update package list
RUN apt-get update
# Upgrade installed packages
RUN apt-get upgrade -y
# Default command (recommended JSON array form)
CMD ["echo", "Hello, World!"]